<div id="registrations-container">
  <h1>Registrations</h1>

  <% if @registrations.present? %>
    <table class="registrations-table">
      <thead>
        <tr>
          <th>First Name</th>
          <th>Last Name</th>
          <th>Birthday</th>
          <th>Gender</th>
          <th>Email</th>
          <th>Phone</th>
          <th>Subject</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @registrations.each do |registration| %>
          <tr>
            <td><%= registration.firstName %></td>
            <td><%= registration.lastName %></td>
            <td><%= registration.birthday %></td>
            <td><%= registration.gender %></td>
            <td><%= registration.email %></td>
            <td><%= registration.phone %></td>
            <td><%= registration.subject %></td>
            <td id="actions-row">
              <%= link_to 'Edit', edit_registration_path(registration), class: 'edit-button' %>
              <%= button_to 'Delete', registration_path(registration), method: :delete, class: 'delete-button' %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p class="no-registrations-message">No registrations found.</p>
  <% end %>

  <%= link_to 'Back to Register', root_path, class: 'back-to-home-button' %>
</div>
